*** First Try ***
*** Brute Force Method ***

public int[] twoSum(int[] numbers, int target) {
  int[] index ={0,0};
  int len = numbers.length;

  for(int i = 0; i < len-1; i++){
      int goal = target-numbers[i];           

      for(int j = i+1; j < len; j++){     
          if(numbers[j] == goal){                  
              index[0] = i+1;
              index[1] = j+1;
              // End the loop
              i = len;
              j = len;
          } else if(numbers[j] > goal){
              j = len;
          }
      }


  }        
  return index;
}
